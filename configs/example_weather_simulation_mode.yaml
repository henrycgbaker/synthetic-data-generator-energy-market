# Example: Weather Simulation Mode (Default)
# This config showcases: Wind and solar availability calculated from weather models
# Inherits from: _base_template.yaml
#
# KEY FEATURE BEING TESTED: Weather simulation mode for renewable availability

# ===== OVERRIDE: Output Configuration =====
io:
  out_dir: "outputs"
  dataset_name: "weather_simulation_example"
  add_timestamp: true
  version: "v1"
  save_pickle: true
  save_csv: true
  save_meta: true

# ===== All other settings inherit from _base_template.yaml =====
# This example uses default base template settings to showcase weather simulation
# - renewable_availability_mode: "weather_simulation"
# - Wind: AR(1) model with base capacity factor 0.45
# - Solar: Sinusoidal model with peak capacity factor 0.35
# - avail.wind and avail.solar are NOT specified (calculated from weather)
